services:
  nats:
    image: nats:2.10
    command: ["-js"]
    ports:
      - "4222:4222"
      - "8222:8222"

  engine:
    image: ghcr.io/ardo314/ecs-engine/engine:${IMAGE_TAG:-latest}
    build:
      context: ./engine
    depends_on:
      - nats
    environment:
      NATS_URL: nats://nats:4222
    develop:
      watch:
        - action: rebuild
          path: ./engine/Engine

  movement-system:
    image: ghcr.io/ardo314/ecs-engine/movement-system:${IMAGE_TAG:-latest}
    build:
      context: .
      dockerfile: examples/MovementSystem/Dockerfile
    depends_on:
      - nats
      - engine
    environment:
      NATS_URL: nats://nats:4222
    develop:
      watch:
        - action: rebuild
          path: ./examples/MovementSystem

  editor-backend:
    image: ghcr.io/ardo314/ecs-engine/editor-backend:${IMAGE_TAG:-latest}
    build:
      context: .
      dockerfile: editor/backend/Dockerfile
    depends_on:
      - nats
    ports:
      - "5000:5000"
    environment:
      NATS_URL: nats://nats:4222
      BASE_PATH: ${EDITOR_BACKEND_BASE_PATH:-/}
    develop:
      watch:
        - action: rebuild
          path: ./editor/backend/EditorBackend
        - action: rebuild
          path: ./clients/csharp/Client

  editor-frontend:
    image: ghcr.io/ardo314/ecs-engine/editor-frontend:${IMAGE_TAG:-latest}
    build:
      context: ./editor/frontend
    ports:
      - "3000:80"
    depends_on:
      - editor-backend
    environment:
      BASE_PATH: ${EDITOR_FRONTEND_BASE_PATH:-/}
      EDITOR_BACKEND_URL: ${EDITOR_BACKEND_URL:-http://localhost:5000}
    develop:
      watch:
        - action: rebuild
          path: ./editor/frontend/src
        - action: rebuild
          path: ./editor/frontend/package.json
